services:
  - type: web
    name: soulsync-wallpaper-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 3001
